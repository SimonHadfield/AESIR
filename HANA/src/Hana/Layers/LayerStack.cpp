#include "LayerStack.h"

namespace Hana {

	// LayerStack::LayerStack() {};
	// 
	// virtual LayerStack::~LayerStack() {
	// 	for (Layer* layer : layers)
	// 		delete layer;
	// };
	// 
	// const std::vector<Layer*>& LayerStack::GetLayers() const { return layers; }
}